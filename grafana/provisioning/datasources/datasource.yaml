apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: admin
    secureJsonData:
      password: "password123"
    jsonData:
      sslmode: 'disable'
      postgresVersion: 15
    isDefault: true
